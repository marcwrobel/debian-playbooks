---
dependency:
  name: 'galaxy'
driver:
  name: 'docker'
platforms:
  - name: 'debian-10'
    image: 'geerlingguy/docker-${MOLECULE_DISTRO:-debian10}-ansible:latest'
    privileged: true
    pre_build_image: true
provisioner:
  name: 'ansible'
verifier:
  name: 'ansible'
