# {{ ansible_managed }}

deb https://deb.debian.org/debian {{ansible_distribution_release}} main contrib non-free non-free-firmware
deb https://security.debian.org/debian-security {{ansible_distribution_release}}-security main contrib non-free non-free-firmware

# To get updates before a point release is made, see https://www.debian.org/doc/manuals/debian-reference/ch02.en.html
# By default :
# - All installed older packages are upgraded to newer ones from updates.
# - Only manually installed older packages are upgraded to newer ones from backports.
deb https://deb.debian.org/debian {{ansible_distribution_release}}-updates main contrib non-free non-free-firmware
deb https://deb.debian.org/debian {{ansible_distribution_release}}-backports main contrib non-free non-free-firmware
